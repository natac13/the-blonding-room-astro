---
// import Image from 'astro/components/image.astro'
// const images = await Astro.glob('../assets/gallery-2/salon/*.jpg').then(
//   (files) => {
//     return Promise.all(files.map((file) => file.default))
//   },
// )
// console.log(images)
---

<div class="my-20 px-4">
  <ul
    role="list"
    class="grid grid-cols-2 gap-x-4 gap-y-8 sm:grid-cols-3 sm:gap-x-6 lg:grid-cols-5 xl:gap-x-8"
  >
    <!-- {
      images.map((image) => {
        return (
          <li class="relative">
            <div class="aspect-h-7 aspect-w-10 group block w-full overflow-hidden rounded-none bg-gray-100 focus-within:ring-2 focus-within:ring-primary-500 focus-within:ring-offset-2 focus-within:ring-offset-gray-100">
              <Image
                src={image}
                alt="image"
                class="pointer-events-none h-80 w-full object-cover group-hover:opacity-75"
              />
              <a
                href={image.src}
                type="button"
                class="absolute inset-0 focus:outline-none"
              >
                <span class="sr-only">View details for IMG_4985.HEIC</span>
              </a>
            </div>
          </li>
        )
      })
    } -->
  </ul>
</div>
